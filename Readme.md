<!-- // To Run this Code you need to Create an account in Twilio and require SID,AUTH_ID and Service ID
// And set that Ids in .env file

//To send OTP
// In the Postman {{URL}}/sendotp , you need pass a mobile number through body,json or x-form-urlencoded as 'number'

//To Verify OTP
// {{URL}}/verifyotp, pass mobile number and otp through body,json or x-form-urlencoded as 'number' and 'otp' -->